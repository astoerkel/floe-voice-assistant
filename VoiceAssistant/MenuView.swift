import SwiftUI

// Minimal stub to satisfy compilation
struct MenuView: View {
    @Binding var conversationHistory: [Any]
    var apiClient: APIClient
    
    var body: some View {
        Text("Menu View Stub")
    }
}